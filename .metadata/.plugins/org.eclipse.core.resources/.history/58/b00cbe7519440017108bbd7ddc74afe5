package example3;

import java.net.Socket;

public class FileDownThread extends Thread{
	private Socket socket;
	//생성자
	public FileDownThread(Socket socket){
		//인자로 전달받은 객체를 멤버필드에 저장하기
		this.socket=socket;
	}
	//새로운 스레드의 실행순서가 시작되는 run() 메소드
	@Override
	public void run() {
		
	}
}
